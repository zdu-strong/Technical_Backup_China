spring:
  datasource:
    url: jdbc:h2:mem:test?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&createDatabaseIfNotExist=true;MODE=MySQL;DB_CLOSE_DELAY=-1;LOCK_MODE=1;DB_CLOSE_ON_EXIT=FALSE;DEFAULT_LOCK_TIMEOUT=600000
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: com.springboot.project.common.database.CustomH2Dialect
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          "[time_zone]": UTC
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1
  liquibase:
    enabled: false
    change-log: classpath:liquibase/index.xml
  h2:
    console:
      enabled: false
  mail:
    host: smtp.163.com
    username: zdu_202104121637@163.com
    password: WTMZGHSLRALWKLDY
    default-encoding: UTF-8
  main:
    allow-circular-references: true
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  task:
    scheduling:
      pool:
        size: 1000
logging:
  level:
    org:
      hibernate:
        dialect:
          function:
            TemplateRenderer: ERROR
        SQL: DEBUG
        orm:
          jdbc:
            bind: TRACE
server:
  error:
    include-message: always
  tomcat:
    max-http-form-post-size: -1
properties:
  StorageRootPath: defaultTest-a56b075f-102e-edf3-8599-ffc526ec948a
